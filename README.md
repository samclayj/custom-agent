# README

TODO:

- [ ] Fix API key so it isn't in the python file.


Custom environment:

```
python3 -m venv custom-agent-venv
```

Activate:

```
source custom-agent-venv/bin/activate
```

Deps:

```
source custom-agent-venv/bin/activate && pip3 install openai
```
